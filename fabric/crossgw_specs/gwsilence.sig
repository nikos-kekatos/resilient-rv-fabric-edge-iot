# P3.7 gateway silence. One predicate: a verdict observed from a gateway.
# The stream is driven at tick rate, so a timepoint exists even when no
# gateway reports -- that is what makes the absence decidable (G3).
verdict(string)
